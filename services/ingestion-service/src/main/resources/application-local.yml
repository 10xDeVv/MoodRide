spring:
  config:
    activate:
      on-profile: local

moodride:
  elevation:
    opentopodata:
      enabled: true
      base-url: ${MOODRIDE_OPENTOPO_BASE_URL:https://api.opentopodata.org}
      dataset: ${MOODRIDE_OPENTOPO_DATASET:srtm30m}

