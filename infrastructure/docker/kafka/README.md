# Kafka Runtime Assets

This folder contains Docker runtime assets for Kafka-related infrastructure.

- Add topic bootstrap scripts for production-like local runs.
- Keep broker tuning overrides in dedicated files if needed.

